name: naver_api_final
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

  # ▼▼▼ [추가!] 네이버 지도 패키지 ▼▼▼
  flutter_naver_map: ^1.4.1+1
  # ▲▲▲ [추가!] ▲▲▲

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true